name: shards
version: 0.1.0
authors:
  - Ragmaanir <ragmaanir@gmail.com>

dependencies:
  kontrakt:
    github: ragmaanir/kontrakt
    version: ~> 0.2

development_dependencies:
  minitest:
    github: ysbaddaden/minitest.cr
    version: ~> 0.3
  power_assert:
    #github: rosylilly/power_assert.cr
    #version: ~> 0.2
    github: ragmaanir/power_assert.cr
    branch: ahead

license: MIT
